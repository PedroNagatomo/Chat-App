<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.chat.controller.MainController"
            stylesheets="@../css/styles.css"
            style="-fx-background-color: #f8f9fa;">

    <top>
        <VBox spacing="10" style="-fx-background-color: linear-gradient(135deg, #667eea 0%, #764ba2 100%); -fx-padding: 20;">
            <HBox spacing="15" alignment="CENTER_LEFT">
                <Button fx:id="logoutButton" text="Sair"
                        onAction="#handleLogout"
                        style="-fx-background-color: rgba(255,255,255,0.2); -fx-text-fill: white; -fx-border-color: rgba(255,255,255,0.3);"
                        styleClass="button" />

                <Label fx:id="welcomeLabel"
                       style="-fx-font-size: 20px; -fx-text-fill: white; -fx-font-weight: bold;" />

                <Region HBox.hgrow="ALWAYS" />

                <Button fx:id="refreshButton" text="Atualizar"
                        onAction="#handleRefresh"
                        style="-fx-background-color: rgba(255,255,255,0.2); -fx-text-fill: white;"
                        styleClass="button" />
            </HBox>

            <Label text="Salas de Chat Disponíveis"
                   style="-fx-font-size: 14px; -fx-text-fill: rgba(255,255,255,0.9);" />
        </VBox>
    </top>

    <center>
        <SplitPane dividerPositions="0.7" style="-fx-padding: 20;">
            <!-- Painel Esquerdo - Salas -->
            <VBox spacing="15" style="-fx-padding: 0 20 0 0;">
                <VBox spacing="10" style="-fx-padding: 15; -fx-background-color: white; -fx-background-radius: 10; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.05), 10, 0, 0, 2);">
                    <Label text="Criar Nova Sala"
                           style="-fx-font-weight: bold; -fx-font-size: 16px; -fx-text-fill: #333;" />

                    <HBox spacing="10" alignment="CENTER_LEFT">
                        <VBox spacing="5" HBox.hgrow="ALWAYS">
                            <TextField fx:id="roomNameField"
                                       promptText="Nome da sala"
                                       style="-fx-pref-height: 40px;" />
                            <Label text="ex: Sala de Desenvolvimento"
                                   style="-fx-font-size: 11px; -fx-text-fill: #888;" />
                        </VBox>

                        <VBox spacing="5" HBox.hgrow="ALWAYS">
                            <TextField fx:id="roomDescriptionField"
                                       promptText="Descrição (opcional)"
                                       style="-fx-pref-height: 40px;" />
                        </VBox>

                        <Button fx:id="createRoomButton"
                                text="Criar"
                                onAction="#handleCreateRoom"
                                styleClass="button-primary"
                                style="-fx-pref-height: 40px; -fx-pref-width: 80px;" />
                    </HBox>
                </VBox>

                <VBox spacing="10">
                    <Label text="Salas Disponíveis"
                           style="-fx-font-weight: bold; -fx-font-size: 16px; -fx-text-fill: #333;" />

                    <TableView fx:id="roomsTable"
                               style="-fx-background-color: white; -fx-background-radius: 10; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.05), 10, 0, 0, 2);">
                        <columns>
                            <TableColumn fx:id="nameColumn" text="Sala" prefWidth="150" />
                            <TableColumn fx:id="descriptionColumn" text="Descrição" prefWidth="250" />
                            <TableColumn fx:id="usersColumn" text="Usuários" prefWidth="80" />
                        </columns>
                        <columnResizePolicy>
                            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY" />
                        </columnResizePolicy>
                    </TableView>
                </VBox>

                <Button fx:id="joinRoomButton"
                        text="Entrar na Sala Selecionada"
                        onAction="#handleJoinRoom"
                        styleClass="button-primary"
                        style="-fx-pref-height: 45px; -fx-font-size: 14px;"
                        disable="true" />
            </VBox>

            <!-- Painel Direito - Usuários Online -->
            <VBox spacing="15" style="-fx-padding: 0 0 0 20;">
                <VBox spacing="10">
                    <Label text="👥 Usuários Online"
                           style="-fx-font-weight: bold; -fx-font-size: 16px; -fx-text-fill: #333;" />

                    <TableView fx:id="usersTable"
                               style="-fx-background-color: white; -fx-background-radius: 10; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.05), 10, 0, 0, 2);">
                        <columns>
                            <TableColumn fx:id="usernameColumn" text="Usuário" prefWidth="150" />
                            <TableColumn fx:id="statusColumn" text="Status" prefWidth="100" />
                        </columns>
                        <columnResizePolicy>
                            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY" />
                        </columnResizePolicy>
                    </TableView>
                </VBox>

                <VBox spacing="10" style="-fx-padding: 15; -fx-background-color: white; -fx-background-radius: 10; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.05), 10, 0, 0, 2);">
                    <Label text="💡 Dicas Rápidas"
                           style="-fx-font-weight: bold; -fx-font-size: 14px; -fx-text-fill: #333;" />

                    <VBox spacing="5">
                        <Label text="• Clique duas vezes em uma sala para entrar"
                               style="-fx-font-size: 12px; -fx-text-fill: #666;" />
                        <Label text="• Atualize para ver novos usuários"
                               style="-fx-font-size: 12px; -fx-text-fill: #666;" />
                        <Label text="• Crie salas para diferentes tópicos"
                               style="-fx-font-size: 12px; -fx-text-fill: #666;" />
                    </VBox>
                </VBox>
            </VBox>
        </SplitPane>
    </center>
</BorderPane>